// To find the addition of two polynomials f(x) and g(x) and store the value s(x) = f(x) + g(x) in a list
// To find the product of two polynomials f(x) and g(x) and store the value p(x) = f(x) * g(x) in a list
// Differentiate a polynomial and show the result
// The polynomial should be stored in a singly linked list